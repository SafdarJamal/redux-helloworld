function reducer(state, action) {
  switch (action.type) {
    case 'withdraw_money':
      // do something
      break;
    case 'deposit-money':
      // do something
      break;
    default:
      return state;
  }
}

export default reducer;
